import { MuscleGroupPipe } from './muscle-group.pipe';

describe('MuscleGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new MuscleGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
